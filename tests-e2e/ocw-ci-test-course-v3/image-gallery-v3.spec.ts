import { imageGallerySuite } from "../shared/image-gallery.suite"

/**
 * course-v3's image-gallery coverage. The tests themselves live in
 * ../shared/image-gallery.suite.ts, shared with course-v2; this spec runs them
 * under the course-v3 online and offline projects.
 */
imageGallerySuite()
