import { imageGallerySuite } from "../shared/image-gallery.suite"

/**
 * course-v2's image-gallery coverage. The tests themselves live in
 * ../shared/image-gallery.suite.ts, shared with course-v3; this spec runs them
 * under the course-v2 online and offline projects.
 */
imageGallerySuite()
